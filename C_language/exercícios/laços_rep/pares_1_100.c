#include <stdio.h>
#include <stdlib.h>
    int main(){
        system("cls");
    
    int cont = 0;
    while (cont <= 100)
    {
        printf("%i \n", cont);
        cont = cont + 2; 
    }

    return 0;
}